# Build and distribution
/dist
/build

# Dependencies
/node_modules
/bower_components

# IDE and editor files
.vscode/*
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace
.idea/
*.sublime-workspace

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS files
.DS_Store
Thumbs.db

# Written by tools/stamp-version.mjs on every build. Not committed: a local build stamps
# the commit it came from, so a tracked copy would show a diff after every build and its
# content would be wrong for anyone else's checkout.
src/build-version.generated.ts
